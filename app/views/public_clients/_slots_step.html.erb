<% if @selected_enseigne.present? && @selected_service.present? && @date.present? %>
  <div class="booking-section">
    <h2 class="booking-section-title">4. Choisissez un créneau</h2>
    <p class="booking-section-meta">
      <strong><%= @selected_enseigne.name %></strong> — <strong><%= @selected_service.name %></strong> — <%= public_client_formatted_date(@date) %>
    </p>

    <% if @slots.any? %>
      <div class="slot-grid">
        <% @slots.each do |slot| %>
          <%= form_with url: service_bookings_path(@client.slug, @selected_service), method: :post, local: true do %>
            <%= hidden_field_tag :enseigne_id, @selected_enseigne.id %>
            <%= hidden_field_tag :date, @date %>
            <%= hidden_field_tag :start_time, slot %>
            <%= submit_tag public_client_slot_label(slot), class: "slot-button" %>
          <% end %>
        <% end %>
      </div>
    <% else %>
      <p>Aucun créneau n'est disponible pour cette date. Veuillez choisir une autre date.</p>
    <% end %>
  </div>
<% end %>

