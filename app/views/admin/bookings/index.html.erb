<% content_for :hide_booking_back_link, true %>

<div class="booking-header">
  <h1 class="booking-header-title">Espace admin</h1>
  <p class="booking-header-subtitle">Vue globale des reservations de la plateforme</p>
</div>

<div class="booking-summary">
  <div class="booking-summary-row">
    <span class="booking-summary-label">Reservations affichees</span>
    <span class="booking-summary-value"><%= @bookings.size %></span>
  </div>
</div>

<div class="booking-actions">
  <div class="booking-actions-primary">
    <% if @reserve_path.present? %>
      <%= link_to "Reserver", @reserve_path, class: "btn-primary btn-primary--compact" %>
    <% else %>
      <p class="booking-section-meta">Aucun client disponible pour demarrer une reservation.</p>
    <% end %>
  </div>
  <div class="booking-actions-secondary">
    <%= link_to "Gérer les Clients", admin_clients_path, class: "booking-topbar-link" %>
  </div>
</div>

<div class="booking-section">
  <div class="booking-section-header">
    <h2 class="booking-section-title">Dernieres reservations</h2>
  </div>
  <div class="booking-section-content">
    <div class="booking-table-wrapper">
      <table class="booking-table">
        <thead>
          <tr>
            <th>Client</th>
            <th>Enseigne</th>
            <th>Service</th>
            <th>Statut</th>
            <th>Date</th>
            <th>Début</th>
            <th>Fin</th>
            <th>Créé le</th>
            <th>User ID</th>
          </tr>
        </thead>
        <tbody>
          <% @bookings.each do |booking| %>
            <tr>
              <td><%= booking.client.name %></td>
              <td><%= booking.enseigne.name %></td>
              <td><%= booking.service.name %></td>
              <td><span class="booking-status-pill"><%= booking.booking_status %></span></td>
              <td><%= format_date_label(booking.booking_start_time) %></td>
              <td><%= format_time_label(booking.booking_start_time) %></td>
              <td><%= format_time_label(booking.booking_end_time) %></td>
              <td><%= format_datetime_label(booking.created_at) %></td>
              <td><%= booking.user_id || "-" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
