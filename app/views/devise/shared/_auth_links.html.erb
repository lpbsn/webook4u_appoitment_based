<div style="display: flex; gap: 8px; align-items: center; margin-top: 12px;">
  <% if controller_name != "sessions" %>
    <%= link_to "Se connecter", new_session_path(resource_name, redirect_to: params[:redirect_to].presence || session[:after_auth_redirect_to]) %>
  <% end %>

  <% if devise_mapping.registerable? && controller_name != "registrations" %>
    <%= link_to "Créer un compte", new_registration_path(resource_name, redirect_to: params[:redirect_to].presence || session[:after_auth_redirect_to]) %>
  <% end %>
</div>
